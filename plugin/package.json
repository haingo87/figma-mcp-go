{
  "name": "figma-mcp-go-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build && vite build -c vite.config.main.ts",
    "dev": "vite build --watch & vite build --watch -c vite.config.main.ts"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.96.0",
    "typescript": "^5.6.0",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
